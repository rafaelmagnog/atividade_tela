import 'package:flutter/material.dart';

class AppColors {
    AppColors._();

    static const Color primary = Colors.blue;
    static const Color white = Colors.white;
    static const Color avatarBackground = Color(0xFFBDBDBD);
    static const Color imageOverlayDark = Colors.black87;
    static const Color imageOverlayTransparent = Colors.transparent;
}